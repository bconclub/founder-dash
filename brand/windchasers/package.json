{
  "name": "windchasers-brand",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx concurrently -n \"dashboard,web-agent\" -c \"cyan,cyan\" \"npm run dev:dashboard\" \"npm run dev:web-agent\"",
    "dev:dashboard": "cd dashboard/build && npm run dev:dashboard",
    "dev:web-agent": "cd web-agent/build && npm run dev",
    "build": "npm run build:dashboard && npm run build:web-agent",
    "build:dashboard": "cd dashboard/build && npm run build",
    "build:web-agent": "cd web-agent/build && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
